---
name: Daytime sim 1778147132092 verdict (2026-05-07)
description: Step 7 FAILED (chi² blocks corrections), Step 8 mixed (heading PASS, TD REVIEW), severe position-drift regression vs baseline
type: project
originSessionId: 79610daf-47c4-4b65-8f87-6ada8b7fecc8
---
Sim `tests/sims/simulation_data_1778147132092.json` (uploaded 2026-05-07 12:45 PT; 6.2 MB; 485.8 s walk; 553 m VIO path; closed loop with 51.6 m return gap).

**Step 7 acceptance — FAILED.**
- attempts=726, accepts=9 (BoW + PnP passed), corrections_applied=**0**
- chi2_rejected=90 ≈ 9 accepts × 10 damping frames → **chi² gate at threshold 22.5 in `EKFState::updateAbsolutePose` rejects every damped attempt**
- The rotation-sigma fix (3°→20°) helped accepts go from 0 to 9, but it did NOT unblock the chi² gate. Root cause is the residual r' S^-1 r > 22.5 — either rotation residual > ~95° or position residual > ~84 m at correction time.
- Need: logcat `LC_ABS` lines from this run to see actual r_R, r_p, var_R, var_p, m2 values. Code-only diagnosis cannot disambiguate "residual too big" vs "covariance too tight."

**Step 8 acceptance — MIXED.**
- Criterion 1 (heading): **PASS** — ryaw RMSE 69.02° → 41.71° (40% improvement, need ≥10%); loop hdg gap 6.64° → -75.11° (worse direction but |Δ| larger).
- Criterion 2 (TD offset): **REVIEW** — TD = 41.79 ms; |TD − warmup 2 ms| = 39.8 ms ≫ ±5 ms gate. Could be real hardware pipeline latency, not convergence failure. Analyzer flags this for manual review.
- Extrinsics: 180.000° from R_bc identity, expected for diag(1,-1,-1). OK.

**Concerning regressions vs baseline_walk_001:**
- Loop position gap: 1.79 m → 51.63 m (28× worse)
- Direction flips: 2007 (severe heading jitter)
- Path metric mismatch: VIO path = 552 m, event_summary path = 106 m (cross-check ratio = 0.192) — `total_path_m_` accounting may be broken in Tracker
- VIO/PDR ratio = 0.78× (VIO undershoots stride×steps)
- MSCKF huber_rej=36884 vs updates=22184 — more rejections than updates, residuals huge

**Why:** First daytime sim after rotation-sigma fix on commit 221b22b. Result: rotation gate fix is necessary but not sufficient; chi² gate is the next blocker.

**How to apply:** Step 7 cannot ship. Either widen chi² gate (justify against teleportation safety) OR shrink residuals (better keyframe pose source / drift-aware target injection). Step 8 partial — needs logcat to decide if TD=41.79 ms is real latency.
